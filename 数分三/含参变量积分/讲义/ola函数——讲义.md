# ✅《含参变量积分与欧拉函数》系统讲义  

## 四、引出欧拉函数（Eulerian Integrals）

现在开始重头戏：

**欧拉通过含参变量反常积分，定义了两个重要的特殊函数**：

---

### 🌀 欧拉第一类积分：贝塔函数（Beta Function）

#### 定义：

$$
B(p, q) = \int_0^1 x^{p-1}(1-x)^{q-1}\,dx,\quad (p>0,\,q>0)
$$

🔍 特点：
- 积分在区间 $(0,1)$ 内；
- 当 $p < 1$ 或 $q < 1$ 时，端点处是**无界函数反常积分**；
- 但在 $p, q > 0$ 时，积分收敛。

💡 贝塔函数的本质是一个含两个参数的定积分。

#### 基本性质简记：

| 性质 | 内容 |
|------|------|
| 对称性 | $B (p, q) = B (q, p)$ |
| 与 Gamma 关系（暂不推导） | $B (p, q) = \frac{\Gamma (p)\Gamma (q)}{\Gamma (p+q)}$ |
| 三角函数形式 | $B (p, q) = 2\int_0^{\pi/2} \sin^{2 p-1}\theta \cos^{2 q-1}\theta\, d\theta$ |

📌 小例子：

$$
\int_0^1 \sqrt{x}(1-x)^2\,dx = \int_0^1 x^{3/2}(1-x)^2\,dx = B(5/2, 3)
$$

可转化成阶乘式结构计算。

---

### 🌀 欧拉第二类积分：伽马函数（Gamma Function）

#### 定义：

$$
\Gamma(s) = \int_0^\infty x^{s-1} e^{-x}\,dx,\quad (s > 0)
$$

🎯 目的：**阶乘的推广**

观察：  
当 $s = n \in \mathbb{N}^+$，有：
$$
\Gamma(n) = (n-1)!
$$

✅ 所以 γ 函数像是 $(s-1)!$ 在实数甚至复数范围的扩展！

> 💬 比如 $\Gamma (1/2) = \sqrt{\pi}$，意味着 $(-1/2)! "=" \sqrt{\pi}$

#### 收敛性（重要）

拆解积分：  
$$
\Gamma(s) = \underbrace{\int_0^1 x^{s-1}e^{-x}\,dx}_{I_1} + \underbrace{\int_1^\infty x^{s-1}e^{-x}\,dx}_{I_2}
$$

- $I_1$：当 $s < 1$ 时，$x^{s-1} \to \infty$，但 $e^{-x} \approx 1$，所以看 $\int_0^1 x^{s-1}$ 是否收敛 ⇒ 只要 $s > 0$ 即可；
- $I_2$：虽 $x^{s-1} \to \infty$，但 $e^{-x}$ 衰减极快 ⇒ 总是收敛。

✅ 结论：**γ 函数在 $s > 0$ 上有定义**

---

## 五、伽马函数的基本性质（核心记忆）

| 性质       | 表达                                                          | 说明                                          |
| -------- | ----------------------------------------------------------- | ------------------------------------------- |
| 递推关系     | $\Gamma (s+1) = s\Gamma (s)$                                | 类似阶乘：$n! = n \cdot (n-1)!$                  |
| 特例       | $\Gamma (1) = 1$                                            | 因 $\Gamma (1) = \int_0^\infty e^{-x}dx = 1$ |
| 自然数情形    | $\Gamma (n+1) = n!$                                         | 完美延伸阶乘                                      |
| 中点值      | $\Gamma\left (\frac{1}{2}\right) = \sqrt{\pi}$              | 来自高斯积分，关键结论                                 |
| 余元公式（后续） | $\Gamma (s)\Gamma (1-s) = \frac{\pi}{\sin (\pi s)},\ 0<s<1$ | 高阶结论，体现对称性                                  |

---

## 六、伽马函数怎么来的？怎样证明递推关系？

尝试用 **分部积分**!

$$
\Gamma(s+1) = \int_0^\infty x^s e^{-x}\,dx
$$

令：
- $u = x^s$ → $du = s x^{s-1} dx$
- $dv = e^{-x}dx$ → $v = -e^{-x}$

$$
= [-x^s e^{-x}]_0^\infty + s \int_0^\infty x^{s-1}e^{-x}\,dx
$$

边界项在 0 和 ∞ 都为零（练习验证），于是：

$$
\Gamma(s+1) = s \cdot \Gamma(s)
$$

📍 配合 $\Gamma (1)=1$ ⇒ 一切自然数阶乘可生成！

---

## 七、伽马与贝塔的联系（重点公式！）

这个结论很漂亮！

$$
 B(p,q) = \frac{\Gamma(p)\Gamma(q)}{\Gamma(p+q)}
 $$

👉 意义：  
贝塔函数可以通过两个 γ 函数构造出来。

💡 应用举例：

计算：
$$
\int_0^{\infty} \frac{x^{m}}{1+x^n}\,dx = \frac{\pi}{n} \csc\left(\frac{(m+1)\pi}{n}\right),\quad (n>m+1>0)
$$

解法往往借助 γ + β 函数，再配合余元公式。

但我们先掌握基础转化：

### 示例：用 γ 函数表达 $\int_0^1 x^a (1-x)^b\, dx$

显然：
$$
\int_0^1 x^{p-1}(1-x)^{q-1}dx = B(p,q) = \frac{\Gamma(p)\Gamma(q)}{\Gamma(p+q)}
$$

若 $p, q$ 是正整数，则进一步变成阶乘形式！

---

## 八、γ 函数的连续性与可微性如何保证？💡用含参积分语言回答！

我们重新看定义：

$$
\Gamma(s) = \int_0^\infty x^{s-1} e^{-x}\,dx \quad (s>0)
$$

这其实就是**以 $s$ 为参数的反常积分**！

所以问题转化为：

✅ 能否用含参积分的理论研究 $\Gamma(s)$ 的连续性、可微性？

答案是能，关键靠**一致收敛性分析**。

### ✔️ 事实：

- 对任意闭区间 $[a,b] \subset (0,\infty)$，在该区间内 $\Gamma(s)$的积分关于$ s $ 一致收敛。
- 而导函数对应的积分：
  $$
  \int_0^\infty x^{s-1}(\ln x)e^{-x}\,dx
  $$
  在每个闭区间也是一致收敛的。

因此：
> - $\Gamma (s) \in C^\infty (0,\infty)$
> - 可任意次积分下求导！

---

## 九、典型例题精讲（含参积分 + γ 函数）

---

### ❗例 1：计算 $\int_0^\infty x^3 e^{-2 x}\, dx$

**思路**：标准化为伽马函数形式！

$$
\int_0^\infty x^3 e^{-2x}dx
$$

变量替换：令 $u = 2 x$，则 $x = u/2$，$dx = du/2$

代入：
$$
= \int_0^\infty \left(\frac{u}{2}\right)^3 e^{-u} \cdot \frac{du}{2} = \frac{1}{2^4} \int_0^\infty u^3 e^{-u}du = \frac{1}{16} \Gamma(4) = \frac{1}{16} \cdot 3! = \frac{6}{16} = \frac{3}{8}
$$

✅ 答案：$\boxed{\frac{3}{8}}$

---

### ❗例 2：证明 $\Gamma\left (\frac{1}{2}\right) = \sqrt{\pi}$

经典高斯积分配方法！

我们知道：
$$
\int_{-\infty}^\infty e^{-t^2} dt = \sqrt{\pi}
\Rightarrow \int_0^\infty e^{-t^2} dt = \frac{\sqrt{\pi}}{2}
$$

而计算 $\Gamma (1/2) = \int_0^\infty x^{-1/2}e^{-x}dx$

令 $x = t^2$，则 $dx = 2 t\, dt$

$$
\Rightarrow \Gamma\left(\frac{1}{2}\right) = \int_0^\infty (t^2)^{-1/2} e^{-t^2} \cdot 2t\,dt = 2 \int_0^\infty e^{-t^2}\,dt = 2 \cdot \frac{\sqrt{\pi}}{2} = \sqrt{\pi}
$$

✅ 成立！

---

## 十、课程地图总结：你学会了什么？

```
          含参变量积分
                 |
        ┌────────┴────────┐
       初等情形          反常情形
       (有限区间)       (无限区间/奇点)
           |                  |
       连续性/可导性       一致收敛条件控制
           |                  |
           └───────┬────────┘
                   ↓
         构造特殊函数：Γ(s) 和 B(p,q)
                   ↓
            欧拉函数体系建立
                   ↓
       解决复杂积分、逼近问题、概率应用
```

---

## 十一、学习建议 & 思考题

### ✅ 学习路径推荐：

1. 牢记 γ 和 β 的定义域与基本公式  
2. 练习标准化形如 $\int_0^\infty x^{s-1} e^{-ax} dx = \frac{\Gamma (s)}{a^s}$  
3. 掌握贝塔的标准变形（尤其是三角函数形式）  
4. 会用分部、换元、连接 γ 与阶乘  
5. 了解后续可拓展到复数平面（复变函数）

---

### 🤔 思考题练习（课后自练）：

1. 计算 $\int_0^1 x^2\sqrt{1-x}\, dx$  
2. 证明：$\Gamma (s+1)=s\Gamma (s)$ 对所有 $s>0$ 成立  
3. 求 $\frac{d}{ds} [\Gamma (s)]$ 的表达式  
4. 使用 β 函数证明：  
   $$
   \int_0^{\pi/2} \sin^4\theta\,\cos^2\theta\,d\theta = \frac{\pi}{32}
   $$

---

## 附录：常用公式表（速查）

| 名称 | 公式 |
|------|------|
| Gamma 函数 | $\Gamma (s) = \int_0^\infty x^{s-1}e^{-x}dx,\ s>0$ |
| 递推公式 | $\Gamma (s+1) = s\Gamma (s)$ |
| 阶乘关系 | $\Gamma (n+1) = n!,\ n\in\mathbb{N}$ |
| 关键值 | $\Gamma (1)=1,\ \Gamma (1/2)=\sqrt{\pi}$ |
| Beta 函数 | $B (p, q) = \int_0^1 x^{p-1}(1-x)^{q-1}dx$ |
| Beta-Gamma 关系 | $B (p, q) = \dfrac{\Gamma (p)\Gamma (q)}{\Gamma (p+q)}$ |
| Beta 的三角形式 | $B (p, q)=2\int_0^{\pi/2}\sin^{2 p-1}\theta\cos^{2 q-1}\theta\, d\theta$ |

---


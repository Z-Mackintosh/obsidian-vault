## 📄 原题目

设 $f(x,y)$ 当 $y$ 固定时，关于 $x$ 在 $[a,b]$ 上连续，且当 $y \to y_0$ 时，它关于 $y$ 单调增加地趋于连续函数 $\varphi(x)$，证明

$$\lim_{y \to y_0} \int_a^b f(x,y) \, dx = \int_a^b \varphi(x) \, dx.$$

---

## ✏️ 解答

本题要求证明带参数积分的极限与积分运算可以交换顺序。证明的关键在于利用题设的“单调收敛到连续函数”这一条件，从而推导出收敛的一致性。

### 1. 分析条件和目标

要证明

$$\lim_{y \to y_0} \int_a^b f(x,y) \, dx = \int_a^b \varphi(x) \, dx$$

等价于证明

$$\lim_{y \to y_0} \left[ \int_a^b \varphi(x) \, dx - \int_a^b f(x,y) \, dx \right] = 0$$

即证明

$$\lim_{y \to y_0} \int_a^b (\varphi(x) - f(x,y)) \, dx = 0$$

### 2. 利用 Dini 定理证明一致收敛

我们考察函数列 $f(x,y)$ (将 $y$ 视为指标)。

- **闭区间:** $x$ 的定义域 $[a,b]$ 是一个闭有限区间。
    
- **连续性:**
    
    - $f(x,y)$ 当 $y$ 固定时，关于 $x$ 在 $[a,b]$ 上连续（即 $f(x,y)$ 是一个连续函数序列）。
        
    - 极限函数 $\varphi(x)$ 是一个连续函数。
        
- **单调性:** $f(x,y)$ 关于 $y$ 单调增加地趋于 $\varphi(x)$。
    

根据**Dini 定理**：若在闭有限区间上，一列连续函数单调地逐点收敛于一个连续函数，则收敛必是一致收敛。

因此，当 $y \to y_0$ 时，$f(x,y)$ 在 $[a,b]$ 上一致收敛于 $\varphi(x)$，记为：

$$f(x,y) \rightrightarrows \varphi(x) \quad (y \to y_0, x \in [a,b])$$

### 3. 利用一致收敛性证明结论

由一致收敛的定义，对于任意给定的 $\varepsilon > 0$，存在一个 $\delta > 0$，使得当 $0 < |y - y_0| < \delta$ 时，对于所有的 $x \in [a,b]$，有：

$$|f(x,y) - \varphi(x)| < \frac{\varepsilon}{b-a}$$

现在，我们对积分的差值进行估计：

$$\left| \int_a^b f(x,y) \, dx - \int_a^b \varphi(x) \, dx \right| = \left| \int_a^b (f(x,y) - \varphi(x)) \, dx \right|$$

利用积分的性质（绝对值积分 $\ge$ 积分绝对值），我们有：

$$\le \int_a^b |f(x,y) - \varphi(x)| \, dx$$

将一致收敛的估计代入上式：

$$\le \int_a^b \frac{\varepsilon}{b-a} \, dx$$

$$= \frac{\varepsilon}{b-a} \int_a^b 1 \, dx$$

$$= \frac{\varepsilon}{b-a} (b-a)$$

$$= \varepsilon$$

### 4. 结论

根据极限的定义，由于对于任意 $\varepsilon > 0$，我们都能找到一个 $\delta > 0$，使得当 $0 < |y - y_0| < \delta$ 时，

$$\left| \int_a^b f(x,y) \, dx - \int_a^b \varphi(x) \, dx \right| < \varepsilon$$

所以，极限成立：

$$\lim_{y \to y_0} \int_a^b f(x,y) \, dx = \int_a^b \varphi(x) \, dx$$